---
// Home Page
// ------------
// Description: The home page is a page that displays the most important information.

// Components
// - Layout
import Layout from '../layouts/Layout.astro'

// - Blocks
import Hero from '../components/blocks/hero/CenteredHeroCTA.astro'
//import Brand from '../components/blocks/brand/Marquee.astro'
import ScrollSpySteps from '../components/blocks/onboarding/Steps.astro'
import TestimonialMasonry from '../components/blocks/testimonials/TestimonialMasonry.astro'
import Faq from '../components/blocks/faq/StickyFaq.astro'
import CTA from '../components/blocks/cta/CTAPrimary.astro'

// Content
// - SEO
const SEO = {
	title:
		'Kronix',
	description:
		' Kronix'
}
// - Hero data
const heroData = {
	title: 'Invest Tanpa Ragu, Profit dengan Ilmu.',
	subtitle: 'TANGKAP PELUANG SEBELUM PASAR SADAR',
	buttonLink: '/pricing',
	buttonText: 'Join member!',
	text: 'Di KronixSignals, kami membedah pergerakan IHSG melalui kombinasi Tape Reading dan Bandarmology. Pelajari cara baca Bid-Offer untuk mendeteksi akumulasi raksasa sebelum harga breakout.'
}
// - Brand data

// - Testimonial data
const testimonialData = {
	title: 'Tempat Belajar Trading dari Nol hingga Mandiri',
	subtitle: 'What other are saying'
}
// - FAQ data
const faqData = {
	title: 'Frequently Asked Questions',
	subtitle: 'Everything you need to know'
}
// - CTA data
const ctaData = {
	title: 'Amankan Kursi Anda di KronixSignals',
	text: 'Jangan lagi trading sendirian di tengah volatilitas IHSG. Pelajari cara baca Bid-Offer, analisis Bandarmology, dan temukan narasi aksi korporasi sebelum pasar menyadarinya.',
	buttonLink: '/pricing',
	buttonText: 'Join'
}
---

<Layout title={SEO.title} description={SEO.description}>
	<Hero
		title={heroData.title}
		subtitle={heroData.subtitle}
		buttonLink={heroData.buttonLink}
		buttonText={heroData.buttonText}
	/>
	<ScrollSpySteps />
	<TestimonialMasonry title={testimonialData.title} subtitle={testimonialData.subtitle} />
	<Faq subtitle={faqData.subtitle} title={faqData.title} />
	<CTA
		title={ctaData.title}
		text={ctaData.text}
		buttonLink={ctaData.buttonLink}
		buttonText={ctaData.buttonText}
	/>
</Layout>

